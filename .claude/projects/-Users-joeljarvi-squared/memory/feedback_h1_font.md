---
name: h1 font
description: Always use font-rounded for h1 elements
type: feedback
---

Always use `font-rounded` for `h1` elements.

**Why:** User preference for consistent heading style across the project.

**How to apply:** Any time you write or edit an `h1`, ensure it has `font-rounded` in its className.
